# Minimal OpenSTA smoke test.
# Run from the repository root:
# sta -no_splash -no_init examples/run_sta.tcl

puts "OpenSTA is running inside the VSD Codespace."
puts "STA executable: [info nameofexecutable]"
puts "Tcl version: [info patchlevel]"

exit
